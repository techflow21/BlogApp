{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "FileStorage": {
    "Root": "wwwroot/uploads"
  },
  "Firebase": {
    // Set StorageBucket to enable Firebase Storage (e.g. "my-project.appspot.com").
    // Leave empty to use local wwwroot/uploads storage instead.
    "ServiceAccountKeyPath": "",
    "StorageBucket": ""
  }
}
